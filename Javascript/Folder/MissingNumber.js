var arr = [1,3,4,5]
var res = 0;
for(let i = 0; i<arr.length; i++){
 var sum = (arr.length+1)*(arr.length+2)/2
     res = res + arr[i]
        var ans = (sum-res)
    }

   console.log(ans)

   var arr = [1,2,4,5]
   var res = 0
   for(let i =0; i<arr.length; i++){
        var sum = (arr.length+1) * (arr.length+2)/2
        res = res + arr[i]
        var ans =  (sum -res)
   }
   console.log(ans)

   var arr = [1,2,3,4,6]
   var res = 0
   for(let i = 0; i<arr.length; i++){
        var sum = (arr.length+1) * (arr.length+2)/2
        res = res + arr[i]
        var ans = (sum-res)
}
console.log(ans)
   